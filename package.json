{
  "name": "model-generator",
  "version": "1.0.0",
  "description": "模特图生成器 - Cloudflare Pages 部署",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  }
}
